import * as index from './index'
import * as dom from './dom'

export const task = (name, description, duedate, priority) => {
    return {name, description, duedate, priority}
}

export function removeTask(obj){

    console.log(index.task1[obj])
}

// export default task


